// No service worker is used by this app. This empty file exists only so
// stray browser/extension probes for /sw.js get a 200 instead of tripping
// React Router's "no route matches" dev-server error.
